version: '3.8'

services:
  api:
    build:
      target: production
    environment:
      ASPNETCORE_ENVIRONMENT: Production
    restart: unless-stopped
    develop:
      watch: null

  sqlserver:
    restart: unless-stopped

  redis:
    restart: unless-stopped

  nginx:
    restart: unless-stopped